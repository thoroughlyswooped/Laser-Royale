using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SettingsMenu : MonoBehaviour
{
    //TODO: Ezra: This is a hacky way to do this and should be refactored.
    // We want the settings menu to be active before start of game, so that player prefs can be set correctly
    //private void Start()
    //{
    //    CustomSlider[] sliders = GetComponentsInChildren<CustomSlider>();
    //    foreach(var slider in sliders)
    //    {
    //        slider.Initialize();
    //    }

    //    //gameObject.SetActive(false);
    //}
}
